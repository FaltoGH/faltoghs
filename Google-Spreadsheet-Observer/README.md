# Google-Spreadsheet-Observer

You MUST put src/main/resources/credentials.json before compile.

credentials.json can be obtained from Google Cloud Console.
Its URL maybe starts with "https://console.cloud.google.com/apis/credentials/wizard".

Then compile and run with the following command:

```
gradlew
gradlew run
```

Done!
